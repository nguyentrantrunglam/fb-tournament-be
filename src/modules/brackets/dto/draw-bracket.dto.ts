/**
 * Body for POST /categories/:cid/bracket/draw.
 * Empty MVP — draw mode is auto-detected from Registration.seed field.
 * (If any approved registration has a seed, mode = seeded; else random.)
 */
export class DrawBracketDto {}
